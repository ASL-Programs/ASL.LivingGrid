@page "/process-monitor/thresholds"

<div class="page-container">
    <h2>Canlı Proses Monitoru: Threshold Alerting & Auto-Scale</h2>
    <p>Threshold alerting və auto-scale triggers üçün demo UI və mock data.</p>
    <div class="coming-soon-box">
        <span class="soon-message">Hazırlanır / Çox yaxında!</span>
        <button class="btn btn-primary" @onclick="ShowFeedback">Test Et</button>
    </div>
</div>

@code {
    void ShowFeedback() => ShowToast("Hazırlanır / Çox yaxında!");
    void ShowToast(string msg) {
        Console.WriteLine(msg);
    }
}
