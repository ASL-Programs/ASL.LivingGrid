@page "/process-monitor/actions"

<div class="page-container">
    <h2>Canlı Proses Monitoru: Kill/Restart, Filter, Error Notify</h2>
    <p>Prosesləri UI-dan idarə et, modul/istifadəçi/sessiya filteri və error auto-notify üçün demo.</p>
    <div class="coming-soon-box">
        <span class="soon-message">Hazırlanır / Çox yaxında!</span>
        <button class="btn btn-primary" @onclick="ShowFeedback">Test Et</button>
    </div>
</div>

@code {
    void ShowFeedback() => ShowToast("Hazırlanır / Çox yaxında!");
    void ShowToast(string msg) {
        Console.WriteLine(msg);
    }
}
